// *** WARNING: this file was generated by the Pulumi Terraform Bridge (tfgen) Tool. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";

export interface CollectionMember {
    /**
     * An optional comment describing this member.
     */
    comment?: string;
    /**
     * An optional RFC 3339 timestamp after which the Controller automatically removes this member.
     */
    expires?: string;
    /**
     * The location this member matches. Set exactly one of `ip`, `cidr`, `dns`, or `collection`.
     */
    location: outputs.CollectionMemberLocation;
    /**
     * A human readable name for this member.
     */
    name: string;
}

export interface CollectionMemberLocation {
    /**
     * A CIDR range.
     */
    cidr?: string;
    /**
     * The ID of another collection to nest inside this one.
     */
    collection?: string;
    /**
     * A DNS name.
     */
    dns?: string;
    /**
     * A single IP address.
     */
    ip?: string;
}

export interface DnsExclude {
    /**
     * Internal resource ID.
     */
    id: string;
    /**
     * Name to exclude sending to the upstream server for resolution.
     */
    name: string;
    /**
     * Order when presented with other excluded names in the web interface
     */
    order: number;
}

export interface DnsServer {
    /**
     * The IP address for this DNS server.
     */
    addr: string;
    /**
     * Internal resource ID.
     */
    id: string;
    /**
     * The order for this DNS server.
     */
    order: number;
}

export interface PolicySource {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Internal identifier of the source predicate. Assigned by the provider.
     */
    id: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceAndAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceAndNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceAndOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceAndAndAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceAndAndNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceAndAndOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndAndAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndAndNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndAndOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceAndNorAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceAndNorNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceAndNorOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndNorAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndNorNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndNorOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceAndOrAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceAndOrNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceAndOrOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndOrAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndOrNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceAndOrOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceNorAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceNorNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceNorOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceNorAndAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceNorAndNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceNorAndOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorAndAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorAndNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorAndOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceNorNorAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceNorNorNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceNorNorOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorNorAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorNorNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorNorOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceNorOrAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceNorOrNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceNorOrOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorOrAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorOrNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceNorOrOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceOrAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceOrNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceOrOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceOrAndAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceOrAndNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceOrAndOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrAndAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrAndNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrAndOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceOrNorAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceOrNorNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceOrNorOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrNorAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrNorNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrNorOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match if all of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ands?: outputs.PolicySourceOrOrAnd[];
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match if none of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    nors?: outputs.PolicySourceOrOrNor[];
    /**
     * Match if at least one of the listed operands match. Each operand is a single matcher or a further and/or/nor group.
     */
    ors?: outputs.PolicySourceOrOrOr[];
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrOrAnd {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrOrNor {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface PolicySourceOrOrOr {
    /**
     * Match every device, authenticated or not.
     */
    always?: boolean;
    /**
     * Match any device attached to an authenticated user.
     */
    authenticatedUser?: boolean;
    /**
     * Match a single device by ID.
     */
    device?: string;
    /**
     * Match any device in the device group with this ID.
     */
    deviceGroup?: string;
    /**
     * Match a single user by ID.
     */
    user?: string;
    /**
     * Match any user in the user group with this ID.
     */
    userGroup?: string;
}

export interface ResourceLocation {
    /**
     * A CIDR address reachable from behind your Bowtie Controller.
     */
    cidr?: string;
    /**
     * The ID of a collection whose members this resource should match. Requires the default tagged location format.
     */
    collection?: string;
    /**
     * A DNS name pointing to a resource reachable from behind your Bowtie Controller.
     */
    dns?: string;
    /**
     * The IP address of a resource reachable from behind your Bowtie Controller.
     */
    ip?: string;
}

export interface ResourcePorts {
    /**
     * List of allowed ports.
     */
    collections?: number[];
    /**
     * First element is the low port and second is the high port (range is inclusive).
     */
    ranges?: number[];
}
